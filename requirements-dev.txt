# Test and local-tooling deps only. The Docker image installs
# requirements.txt alone, so pytest/httpx/requests never ship in the
# container that runs in Kubernetes.
-r requirements.txt
pytest==8.3.3
httpx==0.27.2
requests==2.32.3
